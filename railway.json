{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["**"]
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=apps/api/prisma/schema.prisma && node dist/apps/api/main.js",
    "restartPolicyType": "ON_FAILURE",
    "rootDirectory": "apps/api"
  }
}
